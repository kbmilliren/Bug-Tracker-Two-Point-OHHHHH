

<div class="col-md-12">
    <div class="jumbotron">
            <text class="text-center">
                <h1>Spider Web Bug Tracker</h1>
                <h2>Spider Web is a software bug and enhancement tracking software.</h2>
                <p>Demo the software as one of the four user types by clicking the links below</p>
            </text>
        </div>
        
        <div class="center">
            <div class="col-md-3">
                @using (Html.BeginForm("Login", "Account", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { role = "form" }))
                {
                    <fieldset>
                        @Html.AntiForgeryToken()
                        <div class="form-group">

                            @Html.Hidden("Email", "admin@info.com", new { @class = "form-control" })

                        </div>
                        <div class="form-group">

                            @Html.Hidden("Password", "Std982582!", new { @class = "form-control" })

                        </div>
                        <input type="hidden" name="RememberMe" value="true" checked />
                        <button class="btn btn-primary center-block btn-lg" type="submit" role="button">Administrator</button>
                    </fieldset>
                }
            </div>
            <div class="col-md-3">
                @using (Html.BeginForm("Login", "Account", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { role = "form" }))
                {
                    <fieldset>
                        @Html.AntiForgeryToken()
                        <div class="form-group">

                            @Html.Hidden("Email", "pm@info.com", new { @class = "form-control" })

                        </div>
                        <div class="form-group">

                            @Html.Hidden("Password", "Std982582!", new { @class = "form-control" })

                        </div>
                        <input type="hidden" name="RememberMe" value="true" checked />
                        <button class="btn btn-primary center-block btn-lg" type="submit" role="button">Project Manager</button>
                    </fieldset>
                }
            </div>
            <div class="col-md-3">
                @using (Html.BeginForm("Login", "Account", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { role = "form" }))
                {
                    <fieldset>
                        @Html.AntiForgeryToken()
                        <div class="form-group">

                            @Html.Hidden("Email", "developer@info.com", new { @class = "form-control" })

                        </div>
                        <div class="form-group">

                            @Html.Hidden("Password", "Std982582!", new { @class = "form-control" })

                        </div>
                        <input type="hidden" name="RememberMe" value="true" checked />
                        <button class="btn btn-primary center-block btn-lg" type="submit" role="button">Developer</button>
                    </fieldset>
                }
            </div>
            <div class="col-md-3">
                @using (Html.BeginForm("Login", "Account", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { role = "form" }))
                {
                    <fieldset>
                        @Html.AntiForgeryToken()
                        <div class="form-group">

                            @Html.Hidden("Email", "submitter@info.com", new { @class = "form-control" })

                        </div>
                        <div class="form-group">

                            @Html.Hidden("Password", "Std982582!", new { @class = "form-control" })

                        </div>
                        <input type="hidden" name="RememberMe" value="true" checked />
                        <button class="btn btn-primary center-block btn-lg" type="submit" role="button">Submitter</button>
                    </fieldset>
                }
            </div>
        </div>
    <br/>
    <br/>
    <br/>
    <br/>
    <br/>

    <div class="col-md-6">
        <button type="button" class="btn btn-default center-block btn-lg">@Html.ActionLink("Login", "Login", "Account") </button>
    </div>
    <div class="col-md-6">
        <button type="button" class="btn btn-default center-block btn-lg">@Html.ActionLink("Register", "Register", "Account") </button>      
    </div>
       
    </div>


   
 
        




  

